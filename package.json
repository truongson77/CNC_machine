{
  "name": "cnc-mach3-ui",
  "private": true,
  "workspaces": ["server", "client"],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:usb": "concurrently -n server,client -c blue,green \"npm run dev:usb -w server\" \"npm run dev -w client\"",
    "ports": "npm run ports -w server",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
